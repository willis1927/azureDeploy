var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var context_exports = {};
__export(context_exports, {
  Context: () => Context,
  getInstance: () => getInstance
});
module.exports = __toCommonJS(context_exports);
var import_core = require("@opentelemetry/core");
var import_semantic_conventions = require("@opentelemetry/semantic-conventions");
var import_generated = require("../../../generated/index.js");
var ai = __toESM(require("../../../utils/constants/applicationinsights.js"));
var import_Constants = require("../../../Declarations/Constants.js");
let instance = null;
class Context {
  tags;
  static sdkVersion = null;
  static opentelemetryVersion = null;
  static nodeVersion = "";
  constructor() {
    this.tags = {};
    this._loadInternalContext();
  }
  _loadInternalContext() {
    const { node } = process.versions;
    [Context.nodeVersion] = node.split(".");
    Context.opentelemetryVersion = import_core.SDK_INFO[import_semantic_conventions.ATTR_TELEMETRY_SDK_VERSION];
    Context.sdkVersion = ai.packageVersion;
    const prefix = process.env[import_Constants.ENV_AZURE_MONITOR_PREFIX] ? process.env[import_Constants.ENV_AZURE_MONITOR_PREFIX] : "";
    const version = this._getVersion();
    const internalSdkVersion = `${prefix}node${Context.nodeVersion}:otel${Context.opentelemetryVersion}:${version}`;
    this.tags[import_generated.KnownContextTagKeys.AiInternalSdkVersion] = internalSdkVersion;
  }
  _getVersion() {
    if (process.env[import_Constants.ENV_MICROSOFT_OPENTELEMETRY_VERSION]) {
      return `mot${process.env[import_Constants.ENV_MICROSOFT_OPENTELEMETRY_VERSION]}`;
    } else if (process.env[import_Constants.ENV_APPLICATIONINSIGHTS_SHIM_VERSION]) {
      return `sha${process.env[import_Constants.ENV_APPLICATIONINSIGHTS_SHIM_VERSION]}`;
    } else if (process.env[import_Constants.ENV_AZURE_MONITOR_DISTRO_VERSION]) {
      return `dst${process.env[import_Constants.ENV_AZURE_MONITOR_DISTRO_VERSION]}`;
    } else {
      return `ext${Context.sdkVersion}`;
    }
  }
}
function getInstance() {
  if (!instance) {
    instance = new Context();
  }
  return instance;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Context,
  getInstance
});
//# sourceMappingURL=context.js.map
