var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var applicationInsightsContext_exports = {};
__export(applicationInsightsContext_exports, {
  createApplicationInsights: () => createApplicationInsights
});
module.exports = __toCommonJS(applicationInsightsContext_exports);
var import_logger = require("../logger.js");
var import_core_client = require("@azure-rest/core-client");
function createApplicationInsights(credential, options = {}) {
  const host = options.host ?? "https://dc.services.visualstudio.com";
  const apiVersion = options.apiVersion ?? "v2.1";
  const endpointUrl = options.endpoint ?? `${host}/${apiVersion}`;
  const prefixFromOptions = options?.userAgentOptions?.userAgentPrefix;
  const userAgentInfo = `azsdk-js-monitor-opentelemetry-exporter/1.0.0-beta.1`;
  const userAgentPrefix = prefixFromOptions ? `${prefixFromOptions} azsdk-js-api ${userAgentInfo}` : `azsdk-js-api ${userAgentInfo}`;
  const { apiVersion: _, ...updatedOptions } = {
    ...options,
    userAgentOptions: { userAgentPrefix },
    loggingOptions: { logger: options.loggingOptions?.logger ?? import_logger.logger.info },
    credentials: { scopes: options.credentials?.scopes ?? ["https://monitor.azure.com/.default"] }
  };
  const clientContext = (0, import_core_client.getClient)(endpointUrl, credential, updatedOptions);
  return { ...clientContext, apiVersion, host };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createApplicationInsights
});
//# sourceMappingURL=applicationInsightsContext.js.map
