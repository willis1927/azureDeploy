import { IAgentLogger } from "../../types";
export declare class ConsoleWriter implements IAgentLogger {
    log(message?: any, ...optional: any[]): void;
    error(message?: any, ...optional: any[]): void;
}
