import { IAgentLogger, IStatusContract } from "../types";
export declare class StatusLogger {
    private _instrumentationKey;
    private _language;
    private _sdkVersion;
    private _processId;
    private _machineName;
    private _agentLogger;
    constructor(instrumentationKey: string, agentLogger?: IAgentLogger);
    logStatus(status: IStatusContract, cb?: (err: Error) => void): void;
    private _addCommonProperties;
}
