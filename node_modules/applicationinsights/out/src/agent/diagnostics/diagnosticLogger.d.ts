import { IAgentLogger, IDiagnosticLog } from "../types";
import { BaseDiagnosticLogger } from "./baseDiagnosticLogger";
export declare class DiagnosticLogger extends BaseDiagnosticLogger {
    constructor(instrumentationKey: string, agentLogger?: IAgentLogger);
    logMessage(diagnosticLog: IDiagnosticLog): void;
    private _addCommonProperties;
}
