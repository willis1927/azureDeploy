import { LogRecordProcessor, ReadableLogRecord } from "@opentelemetry/sdk-logs";
export declare class AttributeLogProcessor implements LogRecordProcessor {
    private _attributes;
    constructor(attributes: {
        [key: string]: string;
    });
    onEmit(record: ReadableLogRecord): void;
    shutdown(): Promise<void>;
    forceFlush(): Promise<void>;
}
